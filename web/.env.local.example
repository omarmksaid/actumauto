# Touchpoint Center — dashboard env.
# With NEXT_PUBLIC_SUPABASE_URL UNSET, the app runs in demo mode (seeded data, zero config).
# Set all three to hit the real backend.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:3000
